{% extends 'baseA.html.twig' %}

{% block title %}SHow{% endblock %}

{% block body %}
    <h1>l'Utilisateur {{ app.user.prenom }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ app.user.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ app.user.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ app.user.email }}</td>
            </tr>
            <tr>
                <th>Login</th>
                <td>{{ app.user.login }}</td>
            </tr>
            <tr>
                <th>Mdp</th>
                <td>{{ app.user.mdp }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ app.user.adresse }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ app.user.phone }}</td>
            </tr>
            <tr>
                <th>DateNaiss</th>
                <td>{{ app.user.dateNaiss ? user.dateNaiss|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ app.user.sexe }}</td>
            </tr>
            <tr>
                <th>Rating</th>
                <td>{{ app.user.rating }}</td>
            </tr>
            <tr>
                <th>NbArtEch</th>
                <td>{{ app.user.nbArtEch }}</td>
            </tr>
            <tr>
                <th>NbArtPos</th>
                <td>{{ app.user.nbArtPos }}</td>
            </tr>

            <tr>
                <th>Image</th>
                <td><img src="{{ asset('upload/' ~user.image) }}" style=" width:100px; height:100px; " alt=""/></td>
            </tr>
        </tbody>

    </table>




{% endblock %}
